// App theme config (Chakra removed; color mode is handled by ColorModeContext)
const theme = {
  config: {
    initialColorMode: "dark" as const,
    useSystemColorMode: false,
  },
};

export default theme;
